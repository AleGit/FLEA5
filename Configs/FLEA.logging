# debug configuration for tests

# emergency < alert < critical < error < warning < notice < info < debug
# for details see Project/Configs/default.logging.

"---" :: "error"    # minimal log priority, log everything up to error
"***" :: "warning"  # default log priority
"+++" :: "info"     # maximal log priority, log nothing above info

"main.swift" :: "emergency"  # file specific log priority
